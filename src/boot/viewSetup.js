/*
    app.set('views', './views');
    app.set('view engine', 'pug');
    res.send(pug.renderFile(path.join(__dirname, '/views/template.pug'), {
        //header: '<center> <h1> Contact Us </h1> </center>',
        section: detail && detail.length ? detail[0].CONTACTUSEN : undefined,
        footer: detail && detail.length ? detail[0].FOOTEREN : undefined
    }));

    app.get('/apiDocs/api_success', (req, res) => {
        const successRenderer = pug.compileFile(path.join(__dirname, 'success.pug'));
        res.send(successRenderer({
            success: require('../assets/success')
        }));
    });
*/
   